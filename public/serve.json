{
  "$comment": "Read by `npx serve -s dist`. Sets cross-origin isolation so SharedArrayBuffer is available and the fast multi-threaded ffmpeg core can run. Safe: all app assets are same-origin and the ffmpeg core loads via same-origin blob URLs.",
  "headers": [
    {
      "source": "**/*",
      "headers": [
        { "key": "Cross-Origin-Opener-Policy", "value": "same-origin" },
        { "key": "Cross-Origin-Embedder-Policy", "value": "require-corp" }
      ]
    }
  ]
}
